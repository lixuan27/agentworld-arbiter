# AgentWorld — 阶段性报告 2026-08-26

一句话：**我们在研究「把多人世界模型的载体从多路视频历史换成一份带类型的权威状态（typed
authoritative state）」这件事的边界在哪里** —— 它能不能当一个跨环境、可规划、可渲染的统一
模拟接口，以及它的 factorization、随机性、状态覆盖各自决定了什么上限。

本报告只写**已经被测出来的东西**，并把**假设**与**已撤回的结论**分开列。

---

## 1. 这个项目在做什么

主流的世界模型把「世界」编码成像素序列，用视频历史去预测视频未来。我们换一个载体：世界是
一份**结构化的、带类型的状态**（谁在哪、血量多少、旗子被谁拿着、哪面墙被打掉了……），模型
在这份状态上做**自回归预测**，渲染器则是一个与预测完全解耦的、确定性的函数。

这样做的代价是失去一部分「像素级预测的便利」，换来的是三件视频载体**结构上做不到**的事：
状态可以被 snapshot / restore（做反事实实验）、可以被注入（做群体不变性实验）、可以被喂回
真引擎取真值（拿到精确的 rollout 监督）。

于是问题变成：**这个接口的上限由什么决定？** 我们把总误差拆成几块，其中三块可以在**完全不
训练任何模型**的情况下直接量出来：

```
E_total(π) = E_state + E_factorization + E_stochastic(π) + E_coverage + E_estimation
             状态不足   分解不匹配        随机性（依策略）   覆盖不足     容量/优化不够
```

**「模型无关」是这个项目最核心的方法论**：`E_stochastic` 和 `E_factorization` 我们不是用
「A 臂比 B 臂好」去论证的，而是**拿真模拟器分支采样**直接量出来的——从一个状态出发，固定
动作，换 K 个不同的随机种子重放，看结果分布长什么样。这样得到的天花板不受训练预算、种子、
收敛与否的影响，任何人拿同一个模拟器都能复现。

---

## 2. 核心算法与实现

**载体（codec）**。状态被编码成 nibble（4 bit）序列。编码前先做 **verify-then-write**：
每个字段的取值范围、整数性、slot 数、局部性都**从数据里测出来**，写死进 codec；越界直接
`SystemExit` 而不是裁剪。这条纪律的作用是：当模型产生一个语料里从未出现过的值时，我们会
**知道**，而不是悄悄把它 clip 掉。

**模型（LogicEngine）**。约 5–6M 参数的因果 Transformer（d_model=256、6 层、8 头）。同一
份代码在 MinAtar、Snake、Craftax、SkirmishCTF 上训练，**从零开始，不引入任何预训练视频
编码器或 LLM 条件**。

**解码模式（C1 dial）**。两种：`mean-field`（各 record 独立解码）与 `chain`（按交互
component 串联解码）。component 只从 `(S_t, A_t)` 构建，**不看未来真值**。

**外生随机性（C5）**。CTF 里争用由一条**外生优先级置换**裁决（两人抢同一格，恰好一人赢）。
解码时可以 `argmax` 也可以 `sample`。

**EARC**（Engine-Anchored Rollout Correction）。把模型生成的状态**喂回真引擎**取下一步真值，
用于纠正暴露偏差。越界的预测状态**丢弃并计数**，绝不裁剪。

**渲染器**。entity-token cross-attention，与状态预测完全解耦；支持 2.5D 主题切换与多视角。

---

## 3. 训练数据与训练设置

| 环境 | 家族 | 说明 |
|---|---|---|
| Freeway / Breakout / Asterix / SpaceInvaders / Seaquest | MinAtar (gymnax) | 单智能体，5 个 |
| Snake-v1 | jumanji | 两个语料：`clean`（几乎不吃果子）与 `seek`（常吃） |
| Craftax-Classic-Symbolic | craftax | 随机性最丰富（怪物生成、资源、昼夜） |
| SkirmishCTF | **自研引擎** | 规则取自 Melting Pot `paintball__capture_the_flag`；语料 `main`/`dense`/`tight`/`koth`，人口 5.6–20.7 活体 |

**为什么自研 CTF 引擎**：反事实 benchmark 需要 state restore，群体不变性需要 state
injection，EARC 需要把模型状态喂回引擎——官方 Lua 引擎三者全无。

**训练设置**：每个环境一个 specialist，60k 步（`--eval-every 5000`），batch 由显存自适应
（SpaceInvaders 为 476）。另有 5M 的 universal checkpoint 同时承载六个环境，per-env 曝光
step-matched。

---

## 4. 已经测出来的结果

### 4.1 Table 1 — 六个环境的 Bayes 天花板 vs 收敛模型

天花板是**模型无关**的（K=64 真模拟器分支，评测器自己的起点集）；模型是 60k 收敛专家。

| 环境 | H1 天花板/模型/归一 | H4 | H16 | H64 |
|---|---|---|---|---|
| Freeway | 1.000 / 1.000 / **100%** | **100%** | **100%** | 0.985/0.953/**96.8%** |
| Breakout | 1.000 / 1.000 / **100%** | **100%** | 1.000/0.953/**95.3%** | (H32 0.656) |
| Snake | 1.000 / 1.000 / **100%** | 0.970/0.969/**99.9%** | 0.940/0.938/**99.7%** | 0.910/0.906/**99.6%** |
| Seaquest | 0.916 / 0.906 / **99.0%** | 0.689/0.672/**97.5%** | 0.177/0.141/**79.5%** | 0.0024/0.0/⚠️ |
| Asterix | 0.888 / 0.859 / **96.7%** | 0.651/0.547/**84.0%** | 0.047/0.0/⚠️ | 4.5e-6/0.0/⚠️ |
| SpaceInvaders | 1.000 / 0.984 / **98.4%** | **95.3%** | **93.8%** | 1.000/0.781/**78.1%** |

⚠️ = 天花板本身低到 64 个起点分辨不了（期望成功数 0.15 / 3.0 / 0.0003），是**仪器分辨率
地板**，不是模型缺陷。

**最大的一次修正**：SpaceInvaders H64 曾是 18.8%，被当作「最差环境」。重训到 60k 后是
**78.1%**（38 胜 0 负，p<1e-9）——**真因是欠训练，不是方法**。

### 4.2 随机性：天花板是**行为策略**的函数，不是环境的性质

这是本项目最深的一条。我们把「随机事件」定义成**模型无关、领域无关**的量：*一个转移是随机
的 ⟺ 从 `(S,A)` 出发的 K 条真分支不全一致*。策略走进这类转移的频率记作 `p_stoch`。

同一个 Snake：

| 语料 | 12 步内吃到果子 | Bayes 天花板 |
|---|---|---|
| `clean`（几乎不吃） | **0 / 64** | **1.0000** |
| `seek`（常吃） | **51 / 64** | **0.2581** |
| greedy 策略 | 58 / 64 | **0.1562** |

**同一个环境、同一个模拟器，天花板从 1.0000 掉到 0.1562，中间没有任何模型或环境的变化——
变的只是产生数据的策略。** 机制在字段级可定位：吃到果子 → `model_if_ate = 0.0000`，没吃到
→ 0.7692；最差的两个字段恰是 `fruit_position.col/row`（0.266/0.281），而 `head_position`
是 **1.000**。蛇被完美追踪，只有**重生的果子**测不到——因为它在随机空格重生。

**方向是双向的**，取决于环境把随机性放在哪：

- **Snake**：随机性在**目标处**（吃到才重生）⇒ **越称职、`p_stoch` 越高**
- **Breakout**：随机性在**失败处**（漏球才触发）⇒ **越不称职越高**（0.0000→0.0833，天花板
  1.0000→0.6517）

**四类分类（7 环境全测）**：

| 类 | `p_stoch` | 环境 |
|---|---|---|
| I 确定性 | 恒 0 | Freeway、SpaceInvaders |
| II 策略敏感 | 随策略双向变 | Snake（目标侧）、Breakout（失败侧） |
| III 饱和随机 | 恒 1 | Craftax（6/6 格，**一个确定性转移都没有**） |
| IV 人口驱动 II→III | 随人口单调↑并饱和 | SkirmishCTF |

**一条跨 7 环境 × 2 策略的普适**：`model_if_stoch` 几乎处处**恰为 0.0000**（最高 0.1053）
——窗口里只要落进一个随机转移，整状态 exact-match 基本就是零。

**⇒ 报告纪律**：任何 exact-match 数字**不附 `p_stoch` 就不可比**。

### 4.3 多智能体：人口律

CTF 的争用由外生优先级置换裁决，每多一个争夺者就多一次掷币：

| 语料 | 平均活体 | `p_stoch` | 整状态天花板 | player | flag | chunk |
|---|---|---|---|---|---|---|
| main | 6.6 | 0.796 | 0.884 | 0.9977 | 0.9973 | 1.0000 |
| tight | 11.0 | 0.958 | 0.803 | 0.9923 | 0.9988 | 0.9999 |
| **dense** | **20.7** | **1.0000** | **0.626** | **0.9825** | 0.9981 | 0.9994 |

**整状态天花板降 30 个点，而 player 只降 1.6 个点、flag/chunk 平到 0.003 以内。**
⇒ 逐 record 的可预测性几乎没动，动的是**必须同时正确的 record 数量**。

一个反直觉的副产物：**chunk（paint/wall_hp）是争用最集中的地方**——paintball CTF 里最常见
的争用就是两束光漆同一格——但它的天花板全程 0.9994–1.0000。**争用最集中处并不是可预测性
丢失处。**

### 4.4 「相乘」机制：七个环境上没有弱分量（H=1）

| 环境 | 最小逐组归一 | 取到它的组 | 该组天花板 |
|---|---|---|---|
| Freeway | **1.0000** | 5 组全 1.0 | 1.0000 |
| Breakout | **1.0000** | 10 组全 1.0 | 1.0000 |
| SpaceInvaders | **0.9844** | `alien_map` | 1.0000 |
| Asterix | **0.9515** | `entities` | 0.9032 |
| Seaquest | **0.9507** | `divers` | 0.9861 |
| Craftax | **0.9477** | `cows` | **0.3133** |
| SkirmishCTF | **0.9393** | `flag` | 0.9981 |

**七个环境、每一个组，没有一个低于 0.9393。** 而同一批权重、同一批状态上，**整状态归一从
1.00 一直到 0.0000**。最锋利的一格：CTF 的 `dense_chain` 臂在 64 个状态里**一个完整状态都
没对**，却同时处于三类 record 天花板的 **98.1–100.2%**。

⚠️ **诚实边界**：逐 record 率的朴素连乘**不能**在数值上复现整状态 `full_exact`（两个谓词
覆盖的字段不同），所以「相乘」这一主张靠的是**差距的方向与量级**，不是一个拟合出来的乘积。

### 4.5 但这条律**在 horizon 上不成立**，而且失效的分量可以点名

SpaceInvaders 是决定性的格，因为它 `p_stoch=0`、**天花板每个 horizon 都恰是 1.0000**：

| H | 天花板 | 整状态归一 | 最小逐组归一 | 该组 |
|---|---|---|---|---|
| 1 | 1.0000 | 1.0000 | 1.0000 | — |
| 4 | 1.0000 | 0.9792 | **0.9792** | **`e_bullet_map`** |
| 16 | 1.0000 | 0.8333 | **0.8333** | **`e_bullet_map`** |
| 64 | 1.0000 | 0.7708 | 0.8542 | `alien_map` |

**H4 与 H16 上最小逐组归一与整状态归一逐位相同**——每一个错的状态都是因为**同一个组**错。
这是本项目**首个分量级缺陷**，而且它有名字：**敌方子弹网格**。

### 4.6 规划：最强的负结果，以及它的解释与修法

在 Snake 上，模型处于其 Bayes 天花板的 **94.5–100%**（H=1/4/16/64 归一 100.0/99.87/98.05/
94.47%）。在这个动力学质量无可指摘的域上：

| 臂（32 配对起点，参考 = model） | clean 语料 | seek 语料 |
|---|---|---|
| random1 | 3胜4负（p=1.00） | 0胜7负（p=0.016） |
| random8 | **5胜4负（p=1.00，不可区分）** | 1胜5负（p=0.219） |
| **greedy**（三行启发式） | **23胜0负，p<1e-6** | **23胜0负，p<1e-6** |
| **oracle**（同候选数、真模拟器打分） | **19胜0负，p<1e-6** | **14胜0负，p=1.2e-4** |

**「动力学够好 ⇒ 规划够好」被直接证否。**

**机制（算术闭合，两个独立预测各落在 1 个点以内）**：

1. clean 语料**从不吃果子**（`p_stoch=0.0017`，64 个 12 步窗口里 **0** 次）⇒ 模型关于「吃」
   这个事件的信念几乎不受数据约束
2. 信念被高估 **5.5×**（believed 0.127 vs true 0.023）；但**排序能力是好的**
   （Spearman **+0.712**）⇒ 坏的是**离散事件校准**，不是排序
3. 候选分布本身几乎不含成功：均匀 12 步序列吃到果子的真实概率仅 **2.3%**
4. `P(64 个候选至少一个吃到) = 1 − 0.977⁶⁴ = 0.774` → oracle 实测 **0.719**；
   模型认为 8.1 个能吃、真实 1.5 个 ⇒ argmax 命中概率 ≈ **0.19** → model 实测 **0.125**

**修法（F200）**：`--proposal directed` **只改 CEM 的 round-0 先验**，其余全不动：

| 臂 | uniform 提议 | **directed 提议** |
|---|---|---|
| random1 / random8 / greedy | 0.094 / 0.156 / 0.844 | **完全不变**（对照成立） |
| **model** | **0.125** | **0.531（+4.25×）** |
| oracle | 0.719 | 0.844 |

**什么都不训练，只改先验，规划成功率翻 4 倍**；校准同时从 5.5× 修到 **1.34×**——因为定向
候选**真的会吃到**，事件不再稀有。

### 4.7 哪个指标预测规划效用（H7 判决）

同一个 job 内，五个 planner 臂里**不依赖模型的四个逐位相同**，只有 model 臂动。同语料
15k→60k：

| 指标 | 15k | 60k | 变化 |
|---|---|---|---|
| exact-match @recorded 动作 H12 | 0.9688 | 1.0000 | +0.031（**已近饱和**） |
| exact-match @**greedy** 动作 H12 | 0.0469 | 0.0469 | **+0.000（被天花板钉死）** |
| **scorer Spearman** | +0.712 | **+0.794** | |
| **事件校准 believed/true** | **5.5×** | **3.3×** | |
| **scorer regret vs best** | +0.317 | **+0.184** | **−42%** |
| **规划成功率** | 0.125 | **0.219** | **+75% 相对** |

**⇒ 预测规划效用的是 scorer 层指标（regret、事件校准比），不是状态精度层指标。**

### 4.8 其它已确立的结果

- **载体对比**：dense 长时域 occupancy 反超（0.80 vs 0.36）；video 载体 X-view 不一致
  0.21–0.25 而 typed **恰为 0**
- **渲染**：~95% 感知误差来自**动力学**，renderer 近透明（真态 LPIPS **0.006**）；
  per-view 渲染延迟 N=16→1024 **平坦 5.2–6.9 ms**
- **跨视角一致性**：teacher 对照 **100.00% 位级一致**；learned renderer 共享世界格
  MAE 1.95/255；**learned/pred 与 learned/true 统计上不可区分** ⇒ 状态预测层为视角一致性
  引入的额外漂移 ≈ 0
- **统一接口**：一个 **5M** checkpoint 承载六个模拟器，配对下**逐格与六个独立专家不可区分**
- **系统面**：learned authoritative server「在模型里玩」**5.1 fps**（static-KV + cudagraphs
  3.99×，两种配置**位级一致**）
- **分解**：模型无关的 `C1_joint − C1_meanfield` **≤ +0.0113 处处成立**；
  state sufficiency **1.0000 处处成立**

---

## 5. 已经被撤回的结论（照实列出）

| 撤回项 | 原因 |
|---|---|
| 「4× 数据把 SpaceInvaders H64 抬了 +0.1406（p=0.0352）」 | 基线用错。对正确的平凡 60k 基线，两个种子**均未分辨**（一个恰好 5胜5负）；而旧基线本身**显著劣于**平凡 60k（p=0.0225）——整个效应是 checkpoint 选择差 |
| 「off-policy 动作几乎不付代价」 | 依据的 `alive` 列**排除了终止起点**，正是受害最重的那些。用天花板归一的仪器后，**6/7 格退化很大** |
| 「Craftax mobs 有 60–87% 缺口、cows 是最弱格」 | 读了日志里**最后打印**的那张表（旧 checkpoint × 随机 decode），标成了 canonical 模型。真值是**八组全部 ≥0.9477** |
| 「分解偏差 ≤ +0.0103 处处成立」 | Craftax corpus 臂实测 **+0.0113**，上界订正 |
| 「EARC-25% 未分辨」 | 收敛后**已解出有害**（0 胜 15 负，p=0.0001） |
| 早期 JCR 相关的两条证据链 | 指标本身有 BUG，修好后真值读 0.0000 |

---

## 6. 三个 scaling 轴，已关掉两个

SpaceInvaders H64 = 78.1% 是本项目**唯一干净的估计误差目标**（Class I，双策略 `p_stoch=0`，
天花板恰 1.0000，没有随机性可以推卸）。

| 轴 | 结果 |
|---|---|
| **数据**（4×） | **已撤回**（F201）——对正确基线未分辨 |
| **步数**（60k→120k） | **显著有害**（F208）：best −0.0938（0胜6负，p=0.0312）、last −0.1875（1胜13负，p=0.0018） |
| **容量**（20M） | **在跑** |

**★ 一条论文级证据**：120k 全程 teacher-forced exact 读 **0.9893–0.9922**（不低于 60k），
而 rollout H64 显著下降 ⇒ **用来选 checkpoint 的量与被报告的量朝相反方向移动**。这是该现象
第一次被统计解出。

而 F211 已经把「模型某个部件不行」变成了一个**有名字**的东西（`e_bullet_map`），所以若容量
也为空，结论就是：**这个缺口不可由任何常规 scaling 轴到达，必须换架构或目标函数**。

---

## 7. 方法学纪律（每一条都是付过代价换来的）

1. **verify-then-write**：量程从数据测出来，越界 `SystemExit` 而非裁剪
2. **分子分母必须同空间同分布**：最强的自检是「模型超过自己的天花板」——这条信号抓出过四次
   仪器错配
3. **不变量指标必须分 regime 喂真值**，且守卫必须调**生产代码**而非副本
4. **收敛必须在被报告的指标上判**，不能在 teacher-forced 曲线上判
5. **别让代理指标选出的 checkpoint 独自承担头条**——同一条权重轨迹上，选哪个 checkpoint 能
   把头条改动至多 **31 个点**
6. **每个数字必须带出处**：`ckpt` 与 `decode mode` 钉进表格标题；「日志里最后那张表」不是出处
7. **守卫的阈值必须由它要检测的失效模式推出**——我写的一个守卫曾用「分母的精度」当阈值，
   对同一批数据在两个样本量下给出**相反判决**
8. **均值不是判决**：低于 20pp 的臂间差必须带 McNemar 精确配对

---

## 8. 正在跑的任务

| Job | 内容 | 进度 |
|---|---|---|
| **261593** arm B | SpaceInvaders **20M 容量**（d384/L12 @60k，与归档 `cap20M` 同架构，唯一变量是步数）——**三个 scaling 轴的最后一个** | 约 step 20k/60k，3.0 steps/s，**约 3.7h** |

其余今日提交的作业（policy-ceiling 普查、CTF 人口律、逐组表、horizon 判决、规划系列）**均已
完成并记账**。

---

*本报告对应 finding ledger F191–F211（本轮 21 条，其中 6 条是对我方此前结论的更正）。
完整纲领：`docs/research_program_2026-08-25.md`。*
