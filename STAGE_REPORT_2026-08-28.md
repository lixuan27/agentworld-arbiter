# AgentWorld / ARBITER — 阶段报告 2026-08-28

> 上一份：`STAGE_REPORT_2026-08-26.md`。本份覆盖 **F228–F237**（10 条），
> 主题是 **COPY 规则的域内检验** 与 **C1 首次被搬上视角变换基底**。
> 权威计划仍是 `docs/research_program_2026-08-25.md`（§9.2 在途表、§9.5a 本轮 3-D 结果）。

---

## 0. 一句话

本轮没有新的 SOTA 数字，产出的是**四条可复用的判据**和**一条新的标度律**：
分解式（record-wise）世界模型的代价**不在 agent 身上而在共享世界上**，
且这个代价在视角变换基底上比在平面基底上大 **8.6×**；
同时三个独立仪器在同一天各自返回了**与现象无关的干净零**，全部记录在案。

---

## 1. 本轮结论（按重要性）

### F235 ★★★ D_compose 标度律：分解的代价落在共享态上

模型无关测量（无训练、无语料）：每个 tick 用**同一个 exogenous 抽样**重放，
只让 agent *i* 行动、其余 `A_NOOP`，再与联合步比较。

| N | 每 agent 失配 | **D_compose（共享块层）** | 争用组 均值/最大 |
|---|---|---|---|
| 1 | 0.0000 | 0.0000 | — |
| 2 | 0.0000 | 0.0000 | — |
| 4 | 0.0010 | 0.0013 | 2.00 / 2 |
| 8 | 0.0029 | 0.0130 | 2.00 / 2 |
| 16 | 0.0025 | 0.0430 | 2.00 / 2 |
| 32 | 0.0021 (0.0070) | **0.0521** (0.1784) | 2.02 / 3 |
| 48 | 0.0042 (0.0065) | **0.1406** (0.2760) | 2.03 / 3 |
| 64 | 0.0054 (0.0106) | **0.2656** (0.4896) | 2.03 / 3 |

**N=64 时，record-wise 分解对 99.5% 的 agent record 是对的，
却在 26.6% 的 tick 上把共享世界算错。** 两条曲线相差 **49×** 且仍在张开。

⚠️ **括号内为已撤回的旧值（F244）**：整张表原先测在 BREAK 加权的 `mixed_policy` 下，
而语料是 `policy_miner` 生成的——恰好在唯一能争用的动作上翻了一倍，把效应放大 2–3.5×。
幸存：单调超线性、N ≤ 4 恰为 0、争用组仍很小（最大 4）、逐 agent/共享比值 46× → **49×**。
不幸存：「每隔一个 tick 就错」。

- 24×24×12（2.7 倍体积）把曲线右移但不压平（N=32：0.1784 → 0.0807）⇒ **密度定速率，机制定形状**。
- 争用组始终小（最大 3，直到 N=64）⇒ **不是 percolation**，是大量两两争用。
  这与 CTF 普查同形（N=1024 最大组仅 29），也正是 C1 的 **O(K)** 成本论点成立的原因。

⇒ **多 agent 体素臂必须打在 block 层评分**。共享基底没有 owner，
没有任何 per-record 指标为它负责——这就是 JCR 存在的理由。

### F232 前提：3-D 层此前**从未打开过** C1

`voxel_gen.py` 在 `make_world` 调用处写死 `n_agents=1`。
此前 3-D 层的每一个数字（语料 / 天花板 / COPY 臂 / 渲染器）都是单 agent 的，
**没有一条与本项目的核心机制有关**。

引擎结构决定了误差的分裂：
- **移动**按固定 slot 顺序解算且**无 agent 间占位检查** ⇒ 两 agent 可同格，**移动不争用**；
- **挖/放**（step 4）对着**活的** `s.blocks` 逐 agent 迭代 ⇒
  agent 0 挖穿一格会改变 agent 1 在**同一 tick** 的射线命中 ⇒ 争用在此。

### F236 ★★ gap 看不看得见，取决于 tie-break 随不随机

读引擎而不是比数字：

| | 争用如何解算 | 是否进入后继分布 | 分布式 gap |
|---|---|---|---|
| **CTF** | `prio` = `argsort(rng.random(...))` **排列秩**，`_winner_per_group` 仲裁同格移动(L224)与争用涂色(L316) | 是（走随机通道） | **0.01025**（非零） |
| **Voxel** | **固定 slot 顺序** | 否（每条分支相同） | **恒 0.0000** |

**⇒ 可写进论文的设计观察**：把 tie-break 随机化的设计者让 C1 可被普通分布式天花板测到；
用固定顺序的设计者把它对所有此类指标隐藏，**而危害一点没少**。
真实多人服务器压倒性使用确定性 tie-break（slot 序 / 到达序 / entity id）
⇒ **分布式天花板这一族在本工作真正关心的系统上系统性低估分解代价**。

### F237 ★ 按上述预登记去测 CTF 的另一半——**我的预测被推翻**

⚠️ **voxel 列已按 F244 更正**（旧值括号内），两侧现在都测在**各自语料的策略**下：

| N | CTF `mf_disagree` | **CTF D_compose** | Voxel D_compose | 倍数 |
|---|---|---|---|---|
| 8 | 0.0127 | 0.0052 | 0.0039 (0.0130) | **0.75×** |
| 16 | 0.0122 | 0.0156 | 0.0117 (0.0430) | **0.75×** |
| 24 | 0.0127 | 0.0156 | — | — |
| 32 | 0.0148 | 0.0208 | 0.0521 (0.1784) | **2.5×** (旧 8.6×) |

- 预登记为 `D_compose >> 0.01025`，**实测 1.5×**（0.0156 vs 0.01025 @ 匹配密度 20.72 活体）。
  **预测太强。** 正确读法：**CTF 的分布式 gap 抓住了大部分而非全部**，确定性余项再加约 1/3。
  ⇒ **CTF 那批头条数字站得住**（这是事前点名的"令人放心"分支）。
- ⚠️ **动作分布是真混杂**：均匀随机动作下 D_compose 在 N=4/8/16 读**恰好 0.0000**，
  语料 PolicyBank 在 N=16 给 0.0156。**只跑随机臂的话，"放心"结论完全是假象。**
- ⚠️⚠️ **更正后必须撤回一句话：「per-record 指标在 3-D 基底上比在平面基底上误导得多」
  这个说法的依据不再是量级。** 更正后 voxel 在 N=8/16 上**低于** CTF（0.75×），
  只有 N=32 才反超（2.5×，旧值 8.6×）。而且两个基底的世界大小/密度本来就不同，
  **「同一个 N」的跨基底比较本身就是松的**，之前不该按倍数下结论。
- **真正稳健、且更新后更强的是形状**：CTF 逐 player 失配（0.0122–0.0148，随 N 基本平）
  **与共享误差同量级**；voxel 逐 agent 停在 0.0054 而共享误差是它的 **49×**（旧 46×）。
  机制上：**CTF 里争用伤的是争用者**（被打中、抢格子输了），
  **Voxel 里争用伤的是世界**（地形最终不一样，而争用者毫发无损）。
  ⇒ **正确的表述是：在 3-D 基底上，per-record 指标与共享层的误差彻底解耦；
  在 CTF 上两者同量级。** 这是"做视角变换层"的理由，而它是关于**结构**的陈述，不是关于倍数的。
- 另记 `union_ill_defined_cells` 每次 1–6 个：两条 solo 重放对同一格写入**不同**值，
  **根本不存在 union**。稀少但是分解式预测本身的结构性歧义，故**单独计数不做任意消解**。

### F228 ★★ 尺寸假说在**已收集的数据**上已死

按字段数排序七个已评域，COPY 增益**完全非单调**：

| 环境 | 字段数 | COPY 增益（配对，最佳 horizon） |
|---|---|---|
| Asterix | 54 | 0.0000 |
| Freeway | 70 | +0.0313（1 次翻转，未分辨）|
| **Breakout** | **110** | **+0.3438**（H32，22-0，p<1e-5）|
| SpaceInvaders | 312 | **−0.1406**（H64，**有害**）|
| Snake | 444 | −0.0625 |
| voxel | 1843 | **+0.7656**（H1，49-0，p<1e-14）|
| **Seaquest** | **1924** | **0.0000**（全场最大状态，增益恰为零）|

两个赢家是第三小与第二大；两个恰好为零的是最小与最大。
**字段数与增益的 Spearman ρ ≈ 0。** 决定增益的是
**状态中"又大又极少变"结构所占的份额**，不是字段总数。

同时这也**收回**了"delta 免费，到处开就行"的读法——SpaceInvaders 实测**更差**。

### F231 ★ 3-D 的 H1 缺口**不是训练量**

voxel_uniform，COPY 参数化：60k → 120k 把 H1 动了 **恰好 0.0000**
（两次都是 0.765625，`acc_on_changed` 16 位有效数字相同），H4 +0.031、H16 +0.031。
天花板 0.9537 ⇒ **18.8 pp 缺口是容量/架构/表示**。

逐组 H1：`blocks` **0.9062**（最差）> `m_y`/`m_z`/`m_active`/`a_z` 0.9375 >
`m_x` 0.9531 > `a_x`/`a_inv` 0.9688 > `a_y` 0.9844 > `a_active`/`a_hp`/`a_pitch`/`a_yaw`/`tick` 1.0。

⚠️ **容量扫描被刻意押后**：逐组表判不了"答错"与"运气差"，而 blocks/mobs 正是世界随机性所在。
264461 会把误差拆成 `det_wrong`（天花板 1.0，全部可回收）/ `stoch_impossible`
（**没有任何分支产生过**的后继）/ `stoch_in_support`（可达但非众数）。
**用测量选下一波，不用猜。**

### F230 ★ 自由相机头条**不是一个测量**

DMC 六域面板里两行不可解读：cartpole `+12.49 dB`、manipulator `+10.47 dB`
（自由相机看起来比任务相机好 10 dB）。
指纹在同一行：**free spread 在每一列都是 task spread 的 1.7–95×**
⇒ 采样 pose 的内在难度差异巨大，**裸 PSNR 跨 pose 不可比**
（取景空背景的 pose 渲染**任何**状态都能拿高分）。
⇒ 264433 给每个 pose 补两道地板（冻结 t0 状态 / 同 pose 另一场景），头条改为**差分中的差分**。

### F233 / F234 ⚠️⚠️ 同一天三个仪器各自返回了**与现象无关的干净零**

| 仪器 | 报出 | 实际 | 为什么它只能是零 |
|---|---|---|---|
| 碰撞计数代理（"两 agent 瞄同一格"）| **0.0000 @ N=4** | 0.0013 | 漏掉二阶通路：agent 0 挖开的洞让 agent 1 的射线**走更远、命中另一格**——两人从未瞄同一处 |
| mean-field 天花板（分支 exo K 次比模态质量）| **+0.0000 @ 全部 8 个 N** | 0.1784 @ N=32 | **只分支 `exo` 会把 (S,A) 钉死** ⇒ 耦合在每条分支里完全相同；`sample_exo` 的 `ore_roll` 按 agent 独立、`spawn_*`/`move_dir` 按 mob 独立 ⇒ **后继分布按构造可分解**。那个 0 是**设定的恒等式** |
| 组合误差（**均匀随机动作**下）| **0.0000 @ N=4/8/16** | 0.0156 @ N=16 | 随机动作把 player 打散；语料策略把他们赶到一起，而争用发生在那里 |

**三个都是同一种抓法**：对同一现象的第二个测量**完全不一致**——
这不可能，除非其中一个没在测它。

> **规则**：一个 null 只有在你证明了**仪器本可以返回别的东西**之后，才是证据。

---

## 2. 本轮落盘的仪器（都可复用）

| 文件 | 作用 |
|---|---|
| `src/agentworld/craftax/codec_delta.py` | Craftax 两套 wire format 的 COPY 子类（`DeltaCodec` 236f / `DeltaCodecFull` 1530f）+ 共享 `resolve` + `copy_by_group`（把机制主张变成对语料的测量） |
| `tests/test_craftax_delta.py` | 8 项门控，含 **`test_context_half_is_byte_identical`**——单变量主张是关于字节的，所以按字节测而非在 docstring 里断言 |
| `scripts/_probe/voxel_contention.py` | 争用普查 + **D_compose**（反事实重放，同 exo）+ C1 天花板（⚠️ 内含 F234 的失明说明，`ceiling_gap` 明确标注**不可**引作分解代价） |
| `scripts/_probe/ctf_compose.py` | CTF 的 D_compose，`--policy bank\|random` 两臂（动作源是混杂不是细节） |
| `scripts/_probe/voxel_error_decomp.py` | H=1 误差五分类：`det_wrong` / `stoch_modal` / `stoch_in_support` / `stoch_impossible` / `det_correct`。⚠️ **绝不修复预测** |
| `scripts/train_voxel_render.py` | 体素第一人称学习式渲染器，**留出偏航 6/7**，两臂 `ray` vs `embed`，三道地板内建 |
| `scripts/bench_dmc_freecam.py`（改） | 每 pose 加冻结 + 错配两道地板，头条改为差分中的差分 |
| `scripts/_probe/make_figs_0828.py` | 本轮 8 张图，数值全部读自盘上 JSON 或引用过的 log 行 |

---

## 2b. 03:00–04:00 追加：两个预登记结清 + 多 agent 层建成（F238–F241）

**F238｜自由相机加地板后，预登记两半全中，排名反转。**
manipulator `+10.47`（面板最好）→ **`−7.06`（面板最差）**；cartpole `+12.49` → `−1.69`；
fish/finger/hopper 三个负值保持为负。九个域 abort gate 全过。
机制在分量里：自由 pose 下模型只比"什么都没动"好 **+1.99 dB**，任务相机下好 **+9.05 dB**
⇒ **这就是视角过拟合，而裸指标把它报成了全场最好**。
副产物（未预登记）：`views_no_better_than_frozen` 随 horizon 饱和，
humanoid/walker/hopper 到 **H16 全部 8/8** ⇒ 过了那个视界 rollout 从那些视点再赚不到任何东西。
⚠️ 两道地板只有一道有分辨力：赢过无关状态太容易，`mismatch` 系统性宽松，`frozen` 才紧。

**F239｜18.8 pp 缺口 5.1× 可回收 ⇒ 容量扫描放行；且分解把缺陷改了名。**
`det_wrong` **0.1602** vs `stoch_impossible` **0.0312**（miner 一致：0.2148 / 0.0391）。
只看确定性转移：`a_z` **0.0889（最差）** > `blocks` 0.0622 > … > **mobs 全部恰好 0.0000**。
旧逐组表把 mobs 算成模型的错，其实全是世界的随机性；`a_z` 在旧表排中游、这里最差。
`a_z` 是唯一真值是**循环**的字段（重力沿 block 列向下扫）⇒ **深度**是假说、**宽度**是对照。
另记 `stoch_in_support` **两个语料都是 0.0000**——模型从不预测"可达但非众数"的后继，
错的时候产出的是模拟器**根本产生不了**的东西。

**F240｜多 agent 体素语料建成并验证。**
`voxel_crowd8`（15×15×8, N=8, 1962 字段, len 3943, 93.1% 共享, D_compose **0.0039**）与
`voxel_crowd32`（12×12×8, N=32, 1906 字段, len 3879, 61.4% 共享, **D_compose 0.1055**）。
⚠️ 这两个数已按 F244 更正（旧值 0.0156 / 0.2604，测错了动作分布）。
动作块**按 slot 有序**（引擎按 slot 顺序解算争用 ⇒ slot 下标是转移函数的一部分）。
默认仍 `n_agents=1` 且单 agent 路径逐位相同 ⇒ 旧语料全不变。

**F241｜⚠️ 我自己的预登记被自己建的分母推翻了（在臂落地之前）。**
天花板：crowd8 `shared 0.9436 / agent_mean 0.9989`；crowd32 `0.9528 / 0.9991`。
**「per-agent 优于 shared」在训练任何模型之前就是环境的性质** ⇒ 原预登记会被环境自己满足，是空的。
**修订判据 = 天花板归一后的比较**：`agent_mean/0.9989` vs `shared/0.9436`（N=8）。
✅ 回归干净：`voxel_uniform` 重测 `C1_joint 0.9537 / p_stoch 0.1211`，与归档**逐位相同**。
**通则：分母要在预登记之前建，不是之后。**

---

## 3. 在途作业（2026-08-28 01:00，03:40 刷新）

⚠️ **GPU 全满**：`sinfo` 的 `mixed` 只说 CPU 有空；判据 `GresUsed` 显示
**每个非 drain 节点都是 8/8 已分配**。6 个 GPU 作业全部 PENDING。
已把 `--time` 从"保险起见"改成实测估计以争取 backfill
（`scontrol update TimeLimit=`，reason 从 `(Priority)` 翻成 `(None)`）。

| Job | 内容 | 预计运行时长 | 状态 |
|---|---|---|---|
| **264425** | Craftax-full COPY（1530f），与 `craftax_full_60k` 步数+语料双匹配 | ~2.5 h + eval | PENDING |
| **264426** | Craftax-classic COPY（236f），与 `craftax_a1_80k` 双匹配（含四份 x10 语料、argmax exo）| ~3.1 h + eval | PENDING |
| **264433** | DMC 自由相机**加地板**重跑（六域）| ~8 min | PENDING |
| **264451** | 体素第一人称渲染器，留出偏航 6/7，`ray` vs `embed` 两臂 | smoke + 2×20k ≈ 1 h | PENDING |
| **264461** | Voxel H=1 误差五分类（K=32 真分支）| ~15 min | PENDING |
| **264385** | voxel_hunt / miner / small 的 COPY 臂（60k）| ~10 h | PENDING |
| **264603** ★ | **容量扫描·深度臂** d256×L12（假说：`a_z` 是循环，要深度）| ~5 h | PENDING |
| **264604** | **容量扫描·宽度对照** d512×L6（参数更多；宽度赢则 `a_z` 故事作废）| ~8 h | PENDING |
| **264621** ★★ | **多 agent 体素训练** crowd8 + crowd32，按天花板归一后比 `shared` vs `agent` | ~7 h | PENDING |

已完成（CPU，均在 1 分钟内返回）：264433 自由相机加地板 · 264461 误差五分类 ·
264611 多 agent 语料生成 · 264613 ranges 校验 + 语料级 D_compose · 264632 多 agent 天花板 + 单 agent 回归。

**F229（预登记，结果出来前写下）**：264425 在 H1–H16 显著获益，
264426 落在噪声内（|Δ| ≤ 0.03，即 64 个起点中 ≤2 次翻转）。
**证伪条件**：两臂同向移动 ⇒ 效应是尺寸/容量驱动的，
F226 对 F224 的更正必须撤回，F227/F228 需要重述。

---

## 4. 尚未解决 / 下一步

1. **等 264461** 再决定容量扫描（`d512×L6` vs `d256×L12` 的分解已写好但**不提交**）。
2. **多 agent 体素语料**需要 codec 支持 N 个动作字段（现 `flatenv/codec.py` 只编码标量动作，
   `ACT_NIBS=2`）。几天工作量，**排在 264461 之后**。
3. **CTF 的 beam / flag 解算**是否也有 F233 的碰撞代理盲点——待查，**不是结论**。
4. `stoch_impossible` 若占主导，需要一个**能看见它**的训练目标；
   现有 exact-match 把它和 `stoch_in_support` 混在一起，schema 合法性放它过去。

---

## 5. 与参考工作的关系（不作为对手，只作命题印证）

- **MASS**（2608.06257）是本组自己的论文，其相应部分直接并入本项目，不做对比。
- **WorldMind**（`docs/ref_worldmind_analysis.md`）：同一命题、相反方向——
  它保留视频生成器并在中间插 compact state 瓶颈，状态**从生成帧推断**；
  我们直接预测权威状态、渲染器解耦。
  它的 compact state 只能承载相机看见过的东西 ⇒ 信息上限就是 `visible_chance`（本项目实测 0.3007）。
  ⚠️ **但我们这一侧也有一个尚未定性的自身缺口（F231 的 18.8 pp）**，
  两边不能只报对方的上限。
